# Codex participation

`SESSION_EXECUTION_AUTHORIZATION`: the user requested implementation and
publication of issue #20. This session may add one immutable contract and
Protobuf revision, checker tests, fixtures and documentation. It may not store
raw prompts, command output, diffs, secrets or host paths, merge the PR, or
rewrite v0.4 evidence.
